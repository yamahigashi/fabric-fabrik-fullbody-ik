require Math;
require Characters;
//require FABRIK;

require InlineDrawing;



/// \dfgPresetFolder Component
function addLegSolver(
                io FABRIKResolver resolver,
                io DrawingHandle handle,
                in String root,
                in String bone1,
                in String bone2,
                in String bone3 ){

    add3BoneIkSolver( resolver, handle, root, bone1, bone2, bone3);
}
